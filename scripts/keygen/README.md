# keygen

This Go CLI generates fresh keys for server and door lock and injects them into the appropriate repos. Results are `gitignore`-ed, so you are free to run this to generate your own local development keys.

```
go run ./scripts/keygen
```
